{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test --scope @bilalvis01/bs-material-ui",
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish",
    "bs:dev": "lerna run bs:dev --parallel",
    "bs:clean": "lerna run bs:clean",
    "bs:build": "lerna run bs:build",
    "bs-mui-icons:build-material-icon-list": "lerna run build-material-icon-list --scope @bilalvis01/bs-material-ui-icons --stream --no-prefix",
    "docs:next:dev": "lerna run next:dev --scope docs --stream --no-prefix",
    "docs:next:build": "lerna run next:build --scope docs --stream --no-prefix",
    "docs:next:start": "lerna run next:start --scope docs --stream --no-prefix",
    "docs:build-icons": "lerna run build-icons --scope docs --stream --no-prefix"
  },
  "devDependencies": {
    "bs-platform": "7.0.1",
    "lerna": "^3.18.3",
    "@glennsl/bs-jest": "0.4.9"
  }
}
