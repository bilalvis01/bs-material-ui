{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "server:docs": "lerna run server --scope docs --stream --no-prefix"
  },
  "devDependencies": {
    "bs-platform": "^5.2.1",
    "lerna": "^3.18.3"
  }
}
